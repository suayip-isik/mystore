import { Text, SafeAreaView } from 'react-native'
import React from 'react'

const Router = () => {
  return (
    <SafeAreaView>
      <Text>Router</Text>
    </SafeAreaView>
  )
}

export default Router